#summary Developers' Guide - API - Syncless.Tagging.CurrentTime
#labels Phase-Implementation
#sidebar DeveloperAPISidebar

Provides properties which enclose the `DateTime.Now` property. The object created allows for repeated use of the same `DateTime.Now` property value.

= Properties Summary =

|| *Property* || *Description* ||
|| [#CurrentTimeLong CurrentTimeLong] || Gets the current time in long format ||
|| [#CurrentTimeString CurrentTimeString] || Gets the current time in string format DD/MM/YY HH:MM:SS ||

= Properties Details =

== !CurrentTimeLong ==

`internal long` !CurrentTimeLong
        Gets the current time in long format.

----

== !CurrentTimeString ==

`internal long` !CurrentTimeString
        Gets the current time in string format DD/MM/YY HH:MM:SS.

----

= Constructors Summary =

|| *Constructor* || *Description* ||
|| [#CurrentTime_() CurrentTime()] || Creates a new !CurrentTime object ||

= Constructors Details =

== !CurrentTime() ==

`internal` !CurrentTime()
        Creates a new !CurrentTime object.

----